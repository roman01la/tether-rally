/**
 * Browser Configuration - COPY THIS TO config.js AND FILL IN YOUR VALUES
 * 
 * Include this file BEFORE the main index.html script tag to override defaults.
 * Example: <script src="config.js"></script>
 */

// Your Cloudflare Worker URL (serves static assets and TURN credentials)
// Example: 'https://your-app.workers.dev'
window.WORKER_URL = '';

// Your camera WHEP URL (MediaMTX via Cloudflare Tunnel)
// Example: 'https://cam.yourdomain.com/cam/whep'
window.CAMERA_WHEP_URL = '';

// Your control relay URL (Pi via Cloudflare Tunnel)
// Example: 'https://control.yourdomain.com'
window.CONTROL_URL = '';

// YouTube Restreamer (Fly.io)
// Example: 'https://your-app.fly.dev'
window.RESTREAMER_URL = '';
window.RESTREAMER_SECRET = '';

// Track Map Configuration (optional)
// Set this to show a mini-map with car position during races
// The SW (southwest) and NE (northeast) corners define the GPS bounds of the track image
// window.TRACK_CONFIG = {
//   image: '/tracks/mytrack.png',  // Path to track image
//   sw: { lat: 111, lon: 111 },  // Southwest corner GPS coords
//   ne: { lat: 111, lon: 111 }   // Northeast corner GPS coords
// };