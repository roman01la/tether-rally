# Pi Environment Variables - COPY TO .env AND FILL IN YOUR VALUES
# Note: Do NOT use 'export' - systemd EnvironmentFile requires KEY=value format

# Token secret for authentication (must match generate-token.js)
TOKEN_SECRET=your-secret-key-here

# Cloudflare TURN credentials (get from Cloudflare dashboard)
# These are optional - only needed if running update-turn-credentials.sh
TURN_KEY_ID=your-turn-key-id
TURN_KEY_API_TOKEN=your-turn-api-token
